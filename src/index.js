import ThemeProvider, { themeProviderDecorator } from './theme-provider'
import withTheme from './with-theme'

export default {
  ThemeProvider,
  withTheme,
  themeProviderDecorator
}

export {
  ThemeProvider,
  withTheme,
  themeProviderDecorator
}